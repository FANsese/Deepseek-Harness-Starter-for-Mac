# DeepSeek Harness 启动器 · 使用说明（macOS）

> 这是一个**图形化启动器**，双击图标即可使用 DeepSeek 官方的 Harness。
> 它**不包含、不修改**任何 Harness 代码——首次运行时会从官方 npm 源下载安装官方原版内核，和你手动敲命令装到的是同一个东西。

## 三步用起来

1. 解压 zip，把 `DeepSeek Harness.app` 拖进「应用程序」文件夹（也可以放任意位置，不影响使用）
2. **第一次打开必须右键点 App →「打开」→ 再点「打开」**（未做苹果付费公证，系统只拦这一次，之后就可以正常双击了）
   - 若提示"已损坏"，在终端（Terminal）执行：
     ```sh
     xattr -cr "/Applications/DeepSeek Harness.app"
     ```
3. 等它自动安装（首次约 5–30 分钟，取决于网速），完成后浏览器会自动打开界面，按页面引导填入你的 DeepSeek API Key 即可

## 安装期间会发生什么

屏幕**右上角**会出现一个小进度面板（不会抢走你正在打字的窗口的焦点）：

- 自动下载 Node.js 运行时（仅当你电脑上没有 Node 时，约 25MB）
- 从官方源下载并安装 Harness 内核（约 1GB）
- 解析依赖（约 15–20 分钟，这段时间不下载不写盘，属正常，面板会显示"依赖表已解析 N 项"在持续增长）

**想中止安装**：点小面板标题栏的 ✕ 即可，会干净终止，不留残余进程。

## 东西都装在哪

全部在 `~/Library/Application Support/DeepSeekHarness/`（内核、运行时、工作区、日志）。

**完全卸载**：删掉 App + 上面这个文件夹，不留任何残余。

## ⚠️ 安全提醒（务必看）

- 界面只监听本机 `127.0.0.1:3080`，局域网和公网访问不到
- **不要用任何端口转发、内网穿透把它暴露到公网**——Harness 的网页界面没有登录鉴权

## 出问题怎么办

看日志，基本都能定位：
```sh
open -t "$HOME/Library/Application Support/DeepSeekHarness/launcher.log"
```

常见问题：

- **安装很慢/看起来卡住**：解析依赖阶段就是这样（不下载不写盘），看面板上"依赖表已解析 N 项"是否持续增长即可判断它活着
- **双击没反应**：先右键 → 打开；若仍无反应，看上面的日志
- **提示内核安装失败**：重开一次会自动续装（已下载的部分有缓存，不会白下）

## 声明

社区第三方启动器，与 DeepSeek 官方无隶属关系。内核版权归 DeepSeek AI（MIT 协议），本启动器同样以 MIT 开源。
